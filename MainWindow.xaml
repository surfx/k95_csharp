<Window x:Class="K95Controller.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:shell="clr-namespace:System.Windows.Shell;assembly=PresentationFramework"
        Title="K95 G-Keys Controller" Height="650" Width="850" 
        MinWidth="400" MinHeight="400"
        Background="Transparent" AllowsTransparency="True" WindowStyle="None"
        Icon="assets/icons8-game-16.ico">
    
    <WindowChrome.WindowChrome>
        <shell:WindowChrome CaptionHeight="35" ResizeBorderThickness="7" GlassFrameThickness="0" CornerRadius="5"/>
    </WindowChrome.WindowChrome>

    <Window.Resources>
        <!-- Cores exatas do MyPlayer (DarkTheme.xaml) -->
        <Color x:Key="BackgroundColor">#161A22</Color>
        <SolidColorBrush x:Key="BackgroundBrush" Color="{StaticResource BackgroundColor}"/>
        
        <LinearGradientBrush x:Key="TitleBarGradient" StartPoint="0.5,0" EndPoint="0.5,1">
            <GradientStop Color="#3A4659" Offset="0.0"/>
            <GradientStop Color="#161A22" Offset="1.0"/>
        </LinearGradientBrush>
        
        <SolidColorBrush x:Key="AccentBrush" Color="#008080"/>
        <SolidColorBrush x:Key="TextBrush" Color="#E7EDF6"/>
        <SolidColorBrush x:Key="MutedTextBrush" Color="#AAB6C7"/>
        <SolidColorBrush x:Key="ControlBackgroundBrush" Color="#222B39"/>
        <SolidColorBrush x:Key="BorderBrush" Color="#3A4659"/>
        
        <Style TargetType="Label">
            <Setter Property="Foreground" Value="{StaticResource TextBrush}"/>
            <Setter Property="FontWeight" Value="Bold"/>
        </Style>
        <Style TargetType="TextBox">
            <Setter Property="Background" Value="{StaticResource ControlBackgroundBrush}"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush" Value="{StaticResource BorderBrush}"/>
            <Setter Property="CaretBrush" Value="White"/>
            <Setter Property="Padding" Value="5"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
        </Style>
        <Style TargetType="Button">
            <Setter Property="Background" Value="#1E2531"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush" Value="{StaticResource BorderBrush}"/>
            <Setter Property="Padding" Value="10,5"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="{StaticResource AccentBrush}"/>
                </Trigger>
            </Style.Triggers>
        </Style>
        <Style TargetType="GroupBox">
            <Setter Property="Foreground" Value="{StaticResource AccentBrush}"/>
            <Setter Property="BorderBrush" Value="{StaticResource BorderBrush}"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Margin" Value="0,0,0,10"/>
        </Style>
    </Window.Resources>

    <Border BorderBrush="{StaticResource BorderBrush}" BorderThickness="1" CornerRadius="5" ClipToBounds="True" Background="{StaticResource BackgroundBrush}">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="35"/> <!-- Custom Title Bar -->
                <RowDefinition Height="*"/>    <!-- Content -->
            </Grid.RowDefinitions>

            <!-- Custom Title Bar (Replica MyPlayer) -->
            <Grid Grid.Row="0" Background="{StaticResource TitleBarGradient}">
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="10,0">
                    <Image Source="assets/icons8-game-16.ico" Width="18" Height="18" Margin="5,0"/>
                    <TextBlock Text="K95 G-Keys Controller" VerticalAlignment="Center" Margin="5,0" FontWeight="Bold" Foreground="White"/>
                </StackPanel>
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                    <Button Content="–" Width="40" Height="35" Background="Transparent" BorderThickness="0" Foreground="White" 
                            shell:WindowChrome.IsHitTestVisibleInChrome="True" Click="BtnMinimize_Click"/>
                    <Button Content="✕" Width="40" Height="35" Background="Transparent" BorderThickness="0" 
                            shell:WindowChrome.IsHitTestVisibleInChrome="True" Click="BtnClose_Click">
                        <Button.Style>
                            <Style TargetType="Button" BasedOn="{StaticResource {x:Type Button}}">
                                <Setter Property="Foreground" Value="White"/>
                                <Style.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter Property="Background" Value="#E74C3C"/>
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </Button.Style>
                    </Button>
                </StackPanel>
            </Grid>

            <!-- Main Content Area -->
            <Grid Grid.Row="1" Margin="15">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <GroupBox Header="CONFIGURAÇÃO DE SCRIPTS (G-KEYS)" Grid.Row="0" Padding="10">
                    <StackPanel>
                        <Grid Margin="0,4">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="40"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="50"/>
                            </Grid.ColumnDefinitions>
                            <Label Content="G1:" Grid.Column="0"/>
                            <TextBox x:Name="TxtG1" Grid.Column="1" Margin="5,0"/>
                            <Button x:Name="BtnG1" Content="..." Grid.Column="2" Click="BrowseScript_Click" Tag="TxtG1" shell:WindowChrome.IsHitTestVisibleInChrome="True"/>
                        </Grid>

                        <Grid Margin="0,4">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="40"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="50"/>
                            </Grid.ColumnDefinitions>
                            <Label Content="G2:" Grid.Column="0"/>
                            <TextBox x:Name="TxtG2" Grid.Column="1" Margin="5,0"/>
                            <Button x:Name="BtnG2" Content="..." Grid.Column="2" Click="BrowseScript_Click" Tag="TxtG2" shell:WindowChrome.IsHitTestVisibleInChrome="True"/>
                        </Grid>

                        <Grid Margin="0,4">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="40"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="50"/>
                            </Grid.ColumnDefinitions>
                            <Label Content="G3:" Grid.Column="0"/>
                            <TextBox x:Name="TxtG3" Grid.Column="1" Margin="5,0"/>
                            <Button x:Name="BtnG3" Content="..." Grid.Column="2" Click="BrowseScript_Click" Tag="TxtG3" shell:WindowChrome.IsHitTestVisibleInChrome="True"/>
                        </Grid>

                        <Grid Margin="0,4">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="40"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="50"/>
                            </Grid.ColumnDefinitions>
                            <Label Content="G4:" Grid.Column="0"/>
                            <TextBox x:Name="TxtG4" Grid.Column="1" Margin="5,0"/>
                            <Button x:Name="BtnG4" Content="..." Grid.Column="2" Click="BrowseScript_Click" Tag="TxtG4" shell:WindowChrome.IsHitTestVisibleInChrome="True"/>
                        </Grid>

                        <Grid Margin="0,4">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="40"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="50"/>
                            </Grid.ColumnDefinitions>
                            <Label Content="G5:" Grid.Column="0"/>
                            <TextBox x:Name="TxtG5" Grid.Column="1" Margin="5,0"/>
                            <Button x:Name="BtnG5" Content="..." Grid.Column="2" Click="BrowseScript_Click" Tag="TxtG5" shell:WindowChrome.IsHitTestVisibleInChrome="True"/>
                        </Grid>

                        <Grid Margin="0,4">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="40"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="50"/>
                            </Grid.ColumnDefinitions>
                            <Label Content="G6:" Grid.Column="0"/>
                            <TextBox x:Name="TxtG6" Grid.Column="1" Margin="5,0"/>
                            <Button x:Name="BtnG6" Content="..." Grid.Column="2" Click="BrowseScript_Click" Tag="TxtG6" shell:WindowChrome.IsHitTestVisibleInChrome="True"/>
                        </Grid>
                    </StackPanel>
                </GroupBox>

                <DockPanel Grid.Row="1" Margin="0,5">
                    <Label Content="LOGS DE EXECUÇÃO" VerticalAlignment="Center" HorizontalAlignment="Left" Foreground="{StaticResource AccentBrush}" FontSize="14"/>
                    <Button Content="Limpar Logs" Click="ClearLogs_Click" HorizontalAlignment="Right" Width="120" Height="30" shell:WindowChrome.IsHitTestVisibleInChrome="True"/>
                </DockPanel>

                <TextBox x:Name="TxtLog" Grid.Row="2" IsReadOnly="True" VerticalScrollBarVisibility="Auto" 
                         Background="#0A1015" Foreground="#00FF41" FontFamily="Consolas" FontSize="12"
                         TextWrapping="Wrap" BorderBrush="{StaticResource BorderBrush}" Padding="10"
                         VerticalContentAlignment="Top" HorizontalContentAlignment="Left"/>
            </Grid>
        </Grid>
    </Border>
</Window>
